import React from "react";
import { Title } from '@mantine/core';

const Home = () => {

    return (
        <div>
            <Title order={1}>Codestreak</Title>
        </div>
    );
};

export default Home;
